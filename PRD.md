# 证件照裁剪工具 - MVP 需求文档 (PRD)

> 创建时间：2026-03-20  
> 创建者：KOAL 🍔

---

## 1. 项目概述

| 项目 | 内容 |
|------|------|
| **产品名称** | 证件照裁剪工具 / ID Photo Cropper |
| **目标用户** | 需要快速制作证件照的个人用户 |
| **核心价值** | 上传照片 → 智能裁剪 → 下载标准尺寸证件照（30秒内完成）|
| **技术约束** | 纯前端实现，Cloudflare Pages 部署，无后端存储 |

---

## 2. 功能需求（MVP范围）

### 2.1 核心功能

| 功能模块 | 需求描述 | 优先级 |
|----------|----------|--------|
| **图片上传** | 支持点击上传和拖拽上传，文件格式 JPG/PNG，大小限制 5MB | P0 |
| **尺寸预设** | 提供4种常用尺寸：一寸(295×413)、二寸(413×626)、护照(354×472)、微信头像(300×300) | P0 |
| **智能裁剪** | 基于人脸检测自动居中，支持手动微调（移动/缩放/旋转） | P0 |
| **背景处理** | 调用 remove.bg API 抠图，支持白/蓝/红三色背景切换 | P0 |
| **图片导出** | 下载裁剪后的证件照，格式 JPG，质量 90% | P0 |

### 2.2 非功能需求

| 需求 | 说明 |
|------|------|
| 性能 | 页面加载 < 2s，图片处理 < 5s |
| 兼容性 | 支持 Chrome/Safari/Firefox 最新2个版本 |
| 响应式 | 适配桌面端和移动端 |
| 隐私 | 图片仅在浏览器内存处理，不落盘 |

---

## 3. 技术方案

### 3.1 技术栈

| 层级 | 技术选型 | 用途 |
|------|----------|------|
| **框架** | Vue3 + Vite | 前端框架 |
| **裁剪组件** | vue-cropper | 图片裁剪交互 |
| **人脸检测** | face-api.js | 人脸定位，自动居中 |
| **抠图服务** | remove.bg API | 背景移除 |
| **代理层** | Cloudflare Worker | API密钥保护、跨域处理 |
| **部署** | Cloudflare Pages | 静态托管 |

### 3.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ 图片上传组件  │→│ 裁剪预览组件  │→│ 背景选择+导出    │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│         ↓                 ↓                    ↓            │
│    face-api.js      vue-cropper           Canvas合成        │
│    (人脸检测)        (裁剪交互)            (图片导出)        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  Cloudflare Worker (代理层)                  │
│              接收图片 → 调用 remove.bg → 返回结果            │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 用户流程

```
[开始]
   ↓
[上传照片] ──→ 格式/大小校验 ──→ 失败 → 提示错误
   ↓ 成功
[人脸检测] ──→ 自动定位人脸 ──→ 生成裁剪框
   ↓
[选择尺寸] ──→ 一寸/二寸/护照/头像
   ↓
[调整裁剪] ──→ 移动/缩放/旋转（可选）
   ↓
[抠图换背景] ──→ 调用 remove.bg ──→ 选择背景色（白/蓝/红）
   ↓
[预览确认]
   ↓
[下载图片] ──→ Canvas合成 ──→ JPG导出
   ↓
[结束]
```

---

## 5. 界面设计

### 5.1 页面结构

```
┌─────────────────────────────────────────┐
│  Logo + 标题                             │
├─────────────────────────────────────────┤
│                                         │
│         [ 拖拽上传区域 ]                  │
│      点击或拖拽上传照片 (支持JPG/PNG)      │
│                                         │
├─────────────────────────────────────────┤
│  左侧面板        │       右侧面板         │
│  ┌───────────┐   │  ┌───────────────┐   │
│  │           │   │  │  尺寸选择      │   │
│  │  裁剪预览  │   │  │  ○ 一寸        │   │
│  │           │   │  │  ○ 二寸        │   │
│  │  [裁剪框]  │   │  │  ○ 护照        │   │
│  │           │   │  │  ○ 微信头像    │   │
│  └───────────┘   │  ├───────────────┤   │
│                  │  │  背景色选择    │   │
│  [旋转按钮]       │  │  ● 白色 ⚪    │   │
│  [重置按钮]       │  │  ○ 蓝色 🔵    │   │
│                  │  │  ○ 红色 🔴    │   │
│                  │  ├───────────────┤   │
│                  │  │  [ 下载证件照 ] │   │
│                  │  └───────────────┘   │
└─────────────────────────────────────────┘
```

### 5.2 交互细节

| 交互 | 行为 |
|------|------|
| 上传图片 | 显示 loading，同时启动人脸检测 |
| 人脸检测成功 | 自动将裁剪框居中于人脸，缩放至合适的头部比例 |
| 切换尺寸 | 裁剪框比例实时变化，保持当前中心点 |
| 抠图处理 | 显示"处理中"状态，约 2-5 秒 |
| 下载 | 生成 JPG，触发浏览器下载 |

---

## 6. API 设计

### 6.1 remove.bg 代理接口

**Endpoint:** `POST /api/remove-bg`

**Request:**
```json
{
  "image": "base64EncodedImageString"
}
```

**Response:**
```json
{
  "success": true,
  "image": "base64EncodedResultImage",
  "width": 295,
  "height": 413
}
```

### 6.2 尺寸配置

```javascript
const SIZE_PRESETS = {
  'id-1': { name: '一寸照', width: 295, height: 413, ratio: 295/413 },
  'id-2': { name: '二寸照', width: 413, height: 626, ratio: 413/626 },
  'passport': { name: '护照', width: 354, height: 472, ratio: 354/472 },
  'avatar': { name: '微信头像', width: 300, height: 300, ratio: 1 }
}
```

---

## 7. 验收标准

| 验收项 | 标准 |
|--------|------|
| 功能验收 | 上传、裁剪、换背景、下载全流程可正常使用 |
| 性能验收 | 页面首屏加载 < 2s，抠图处理 < 5s |
| 兼容性验收 | Chrome/Safari/Firefox 正常显示和操作 |
| 移动端验收 | iPhone/Android 可正常使用裁剪功能 |
| 错误处理 | 文件过大、格式错误、API失败时有明确提示 |

---

## 8. 开发排期

| 阶段 | 任务 | 预估时间 |
|------|------|----------|
| **Day 1** | 项目初始化 + 图片上传 + vue-cropper 集成 | 2h |
| | 人脸检测集成 + 自动居中逻辑 | 2h |
| **Day 2** | 尺寸预设功能 + 裁剪交互优化 | 2h |
| | Cloudflare Worker 代理搭建 | 1.5h |
| | remove.bg 集成 + 背景替换 | 2h |
| **Day 3** | Canvas 合成 + 下载功能 | 1.5h |
| | UI 美化 + 响应式适配 | 2h |
| | Cloudflare Pages 部署 | 1h |

**总计：约 2-3 天（业余时间）**

---

## 9. 风险与应对

| 风险 | 应对措施 |
|------|----------|
| remove.bg API 额度用完 | 提示用户"今日额度已满"，引导手动上传纯色背景替代 |
| 人脸检测失败 | 允许手动调整裁剪框，不强制依赖人脸检测 |
| 移动端裁剪体验差 | 优先保证桌面端，移动端提供基础功能 |

---

## 10. 后续迭代方向（非MVP）

- [ ] 批量处理（一次上传，生成多种尺寸）
- [ ] 打印排版（6寸相纸排版图）
- [ ] 更多背景色（渐变色、自定义颜色）
- [ ] 美颜/磨皮功能
- [ ] 本地离线版（PWA）
